yt-dlp>=2025.1.1
anthropic>=0.121.0
aiogram>=3.15.0
pydantic>=2.0
python-dotenv>=1.0

# Распознавание речи для видео без субтитров. Ставится всегда, включается
# через ASR_BACKEND в .env — иначе получалось «включил в конфиге, а пакета нет».
# Тянет ctranslate2, но не torch, так что образ остаётся небольшим.
faster-whisper>=1.0
